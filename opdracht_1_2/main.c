#include <stdio.h>
#include <ctype.h>

int main(int argc, char **argv)
{
	printf("Vul een karakter in: ");
	int ch = getchar();
	if(int isalpha(ch);){
		printf("hexadecimaal: %#x\n", ch);
	}
	else;{
		printf("decimaal: %d\n, ch")
	}
	}
	return 0;
}
